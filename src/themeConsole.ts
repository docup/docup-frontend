import { createMuiTheme } from '@material-ui/core/styles';

export const consoleTheme = createMuiTheme({
  palette: {
    primary: {
      light: '#ffff8b',
      main: '#B2DFDB',
      dark: '#c9bc1f',
      contrastText: '#FFFFFF',
    },
  },
});
